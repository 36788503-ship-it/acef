# 贡献指南 / Contributing

感谢参与 ACEF！规范类项目对所有权的"干净"要求比普通开源更高（未来可能捐赠给标准组织），请遵守：

## 贡献流程

1. Fork → 建分支 → 修改 → 提 PR
2. PR 描述：改了什么、为什么、对哪些法规映射有影响
3. Schema 变更必须附带对应示例更新（`examples/` 下），并保证 `jsonschema` 校验通过

## 许可与署名（重要）

- **入站=出站（inbound=outbound）**：你提交的贡献按 Apache-2.0 授权给本项目；你保留自己作品的版权
- **DCO 签署**：每个 commit 请加 `Signed-off-by: 你的实名 <邮箱>`（`git commit -s` 即可），表明你有权提交这些内容
- 严禁提交他人拥有版权且未授权的材料（尤其是法条全文、商业数据库内容——法规映射请用自己的语言概述并注明出处）

## 一致性

- 版本化规则见 README §5；`acef_version` 变更需在 PR 里说明是破坏性还是兼容性
- 对 spec 的 MUST/SHOULD 用语改动需要至少一位维护者复核

## 报安全问题

规范本身的缺陷请直接开 Issue；参考实现的安全问题请私下联系仓库所有者，勿在 Issue 公开细节。
